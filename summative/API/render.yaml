services:
  - type: web
    name: student-gpa-predictor
    runtime: python
    pythonVersion: 3.11
    buildCommand: "pip install --only-binary :all: -r requirements.txt"
    startCommand: "uvicorn prediction:app --host 0.0.0.0 --port 8000"
